/.phpunit.cache
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/.claude

# Local Composer binary — install Composer globally instead of committing it.
/composer.phar

# Local dev docs / scratch (keep out of the repo if you prefer)
# /WASSILI_SETUP.md

# Local DB dump (never commit)
/wassili-dump.sql

# Scratch files
/test.txt

# public/build is committed on purpose: Hostinger shared hosting has no
# Node, so the compiled CSS/JS must ship with the repo.
